{
  "name": "urplant-functions",
  "description": "UrPlant Cloud Functions — plant identification, enrichment, achievements, admin API",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint --ext .ts src/",
    "test": "echo 'No tests yet'"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "axios": "^1.7.0",
    "sharp": "^0.33.0",
    "exifreader": "^4.0.0",
    "uuid": "^10.0.0",
    "zod": "^3.23.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/node": "^20.0.0",
    "@types/uuid": "^10.0.0",
    "eslint": "^9.0.0",
    "firebase-functions-test": "^3.0.0",
    "typescript": "^5.6.0"
  }
}